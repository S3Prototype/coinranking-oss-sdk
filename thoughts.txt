The getDeepResultsList method goes ased off the search query api endpoint's returned details. That endpoint returns no info about data types outside market, exchange, and coins. So we must perform a separate search for each item outside of that, then filter those manually too.